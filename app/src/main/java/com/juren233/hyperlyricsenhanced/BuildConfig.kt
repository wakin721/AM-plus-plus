package com.juren233.hyperlyricsenhanced

/** Compatibility constants required by the HLE metadata source set. */
internal object BuildConfig {
    const val DEBUG: Boolean = false
    const val VERSION_CODE: Int = 103
}
